Each test contains multiple test cases. The first line contains the number of test cases
$t$ ($1 \leq t \leq 10^4$). The description of each test case follows.

Each test case begins with three integers
$n$, $k$ and $x$ ($1 \leq k \leq n \leq 5 \cdot 10^5$, $0 \leq x \leq n$).

The next line contains $n$ integers, $a_1, a_2, \ldots, a_n$ ($1 \leq a_i \leq k$).

It is guaranteed that the sum of $n$ over all test cases does not exceed $5 \cdot 10^5$.
