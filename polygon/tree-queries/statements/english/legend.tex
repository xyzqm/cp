\epigraph{\it{Too tired to write flavor-text...}}{--- Daniel}

Given a tree on $n$ nodes, answer $q$ queries of the following form:
what's the size of the smallest subset of edges that connects all pairs of nodes $(u, v)$ for $u, v \in [l, r]$?
